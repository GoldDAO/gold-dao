import { useWallet } from 'Providers';
import { Button } from '@mui/material';
import React, { useEffect } from 'react';

const BatchOffers = () => {

    // useEffect(() => {
    //     (async () => {
    //         const result = await window.ic.plug.requestBalance(params);
    //         console.log(result);
    //     })();
    // }, [window])

    // const doTransfert = () => {
    //     window.ic[wallet.meta.id].batchTransactions()
    // }

    return (
        <></>
    );
};

export default BatchOffers;