import { IconButton } from '@mui/material';
import React from 'react';
import LogoutIcon from '@mui/icons-material/Logout';

const LogoutButton = () => {
    return (
        <IconButton><LogoutIcon />Logout</IconButton>
    );
};

export default LogoutButton;