FROM node:18

# ENV CANISTER_NAME=

COPY . ./gldt

WORKDIR /gldt

# TODO
# CMD ./scripts/build-canister.sh --wasmonly $CANISTER_NAME && \
#     cp backend/canisters/$CANISTER_NAME/target/wasm32-unknown-unknown/release/$CANISTER_NAME.wasm /tmp/reproductible_build/
