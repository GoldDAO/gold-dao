{
    "name": "gldt-swap-monorepo",
    "description": "GLDT frontend app and GLDT canister",
    "author": "Bochsler Consulting",
    "workspaces": [
        "packages/*"
    ],
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "frontdev": "cd ./packages/client/ && npm run develop",
        "frontbuild": "cd ./packages/client/ && npm run build:front",
        "deploy": "echo \"This command may be replaced by the 'version' script in the future.\" && dfx identity use yumi-fundation && npm run lint && npm run build && dfx deploy --network=ic",
        "start": "cd packages/client && dfx start --background --clean && npm run build && dfx deploy",
        "stop": "cd packages/client && dfx stop",
        "restart": "cd packages/client npm run stop; npm start",
        "frontlint": "cd packages/client next lint",
        "test": "echo \"No tests yet! Please write some unit tests!\""
    },
    "devDependencies": {
        "eslint": "8.40.0"
    }
}